import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: '#ccc',
    borderWidth: 1,
    flexDirection: 'row',
    marginTop: 5,
    marginBottom: 5,
  },
  input: {
    flex: 1,
    textAlign: 'center',
  }
});

export default styles;
